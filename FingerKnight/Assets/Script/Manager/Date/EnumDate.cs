using System.Collections;
using System.Collections.Generic;

public static class EnumDate {
    public enum E_UIList { MAIN, LOBY, UPGRADE, CHARACTER, STAGE, SELECT, GAME };
    public enum E_Character { Knight, Warrior};
    public enum E_CharState { NONE, MOVE, ATTACK, DEFEND, DIE};
}
